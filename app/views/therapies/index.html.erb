<main class="main">
  <div class="inner">
    <%# ログインしているときは以下を表示する %>

      <% if user_signed_in? %>
      <div class="greeting">
        <%= "こんにちは、" %>
        <%= link_to "#{current_user.nickname}さん", user_path(current_user.id),class: :greeting__link%>
       </div>
       <h3><%= link_to '商品検索', search_therapies_path%></h3>
       <% end %>

    <%# // ログインしているときは上記を表示する %>

    <div class="card__wrapper">
    <%= render partial: 'therapy', collection: @therapies %>
      <%# 投稿機能実装後、部分テンプレートでプロトタイプ投稿一覧を表示する %>
    </div>
  </div>
</main>
